[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Note: The hidden static form file (public/hidden-form.html) allows Netlify
# to detect form fields. If Netlify auto-installs @netlify/plugin-nextjs,
# you'll need to remove it from Site settings → Plugins in the Netlify UI.

